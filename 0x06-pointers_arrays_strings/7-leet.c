#include "holberton.h"
/**
* *leet - return char
*
* @d: value
*
*
* Return: Always 0 (Success)
*/
char *leet(char d*)
{
	char narray[];
	for (count = 0; d[count] ; count++)
	{	
			
	}
	return (d);
}
