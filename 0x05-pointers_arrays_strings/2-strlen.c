#include "holberton.h"
#include "stdio.h"
/**
 * _strlen - check the code for Holberton School students.
 * @s: pointer
 *
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int index;

	for (index = 0; s[index] != '\0' ; index++)
	{
	}
	return (index);
}
